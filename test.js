// from index import {Subtitles}
let assert = require("assert")

describe("Load subtitles", () => {
  describe("Search for a movie", () => {

    it("Should find results for a movie", (done) => {

      new Promise((resolve) => {
        assert.ok(true)

        // Subtitles.search("comedy")

        resolve()
      }).then(done)

    })
  })
})
